# Facilitator Server Configuration
PORT=3001
SERVICE_ID=1

# ZK Credential Signing (BN254 Schnorr)
# Secret key for signing credentials - generate a secure random value for production
FACILITATOR_SECRET_KEY=0x...

# EVM Payment Settlement
# Private key for executing USDC transfers (secp256k1)
# Only required when ALLOW_MOCK_PAYMENTS=false
FACILITATOR_PRIVATE_KEY=0x...

# Chain Configuration
CHAIN_ID=84532
RPC_URL=http://localhost:8545

# Payment recipient address (where USDC is transferred to)
RECIPIENT_ADDRESS=0x...

# Development: skip real on-chain settlement
ALLOW_MOCK_PAYMENTS=false
